- clean: ['~']

- link:
    ~/.vimrc: vim/vimrc
    ~/.vim: vim
    ~/.bash_colors:
      force: true
      path: bash_colors
    ~/.bashrc:
      force: true
      path: bashrc
    ~/.gitconfig: gitconfig
    ~/.ssh/config:
        path: ssh/config
        create: true
    ~/.tmux.conf: tmux.conf
