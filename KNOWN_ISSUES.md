## Can't add Connection UUID already exists

Update the BIOS firmware for ASUS ROG STRIX motherboard

## To Make vim copy paste work through ssh

```
sudo vim /etc/ssh/sshd_config
```

Add: 

```
X11Forwarding yes
X11UseLocalhost no
```
